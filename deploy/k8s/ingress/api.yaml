apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-api
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.wildside.cc
      secretName: myapp-api-tls
  rules:
    - host: api.wildside.cc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myapp-backend
                port:
                  name: http
