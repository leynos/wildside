replicaCount: 1

nameOverride: ""
fullnameOverride: ""

image:
  repository: ghcr.io/acme/myapp-backend
  tag: ""
  # pullPolicy: IfNotPresent

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

pdb:
  enabled: true
  minAvailable: 1

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

ingress:
  enabled: false
  className: "traefik" # Default to your new controller
  hostname: "api.wildside.cc"
  tlsSecretName: "myapp-api-tls"
  annotations: {}

# You can also include default env vars, secrets, etc.
config:
  APP_ENV: "development"
secretEnv: {}

affinity: {}

securityContext: {}

container:
  livenessProbe: {}
  readinessProbe: {}
  startupProbe: {}
