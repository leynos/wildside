# Valkey module
#
# Deploys a Valkey cluster (Redis-compatible) using the Hyperspike Valkey
# operator to provide high-availability key-value storage for the Wildside
# platform. Supports TLS via cert-manager and integrates with the vault_eso
# module for credential management via External Secrets Operator.
