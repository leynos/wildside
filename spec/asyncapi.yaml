asyncapi: 2.6.0
info:
  title: Wildside WebSocket API
  version: 0.1.0
  description: Minimal AsyncAPI document for WebSocket channel readiness.
servers:
  production:
    url: wss://ws.yourdomain.example/
    protocol: wss
channels:
  /ws:
    publish:
      summary: Client submits a display name for validation.
      message:
        payload:
          type: string
