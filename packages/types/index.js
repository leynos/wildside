/**
 * @file Runtime stub for @app/types to satisfy Node's module resolution.
 */
export {};
// Expose a benign default to avoid runtime errors if a default import slips in.
export default undefined;
