# Diesel CLI configuration.
#
# This file configures the diesel CLI tool for schema generation and
# migration management. Run `diesel print-schema` to regenerate the
# schema file from a live database.

[print_schema]
file = "src/outbound/persistence/schema.rs"
custom_type_derives = ["diesel::query_builder::QueryId"]

[migrations_directory]
dir = "migrations"
